setting文件里第一行是输出目录，第二行是版本
mod文件夹内是每个版本的文件夹，版本文件夹内是各个mod和LS文件，每行是一个mod的名字和依赖，用空格分隔
group文件夹内和mod结构一样，版本文件夹内是各个mod包和LS文件，每行是一个mod包的名字
mod包内有m x(安装名为x的mod)和g x(安装名为x的mod包),每行一个

不能自动更新
mod多的话可能需要很长时间来处理文件
一开始是给mc写的，但是其他地方可能也有些用